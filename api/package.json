{
  "name": "kasparro-api",
  "type": "module",
  "scripts": {
    "prisma:migrate": "dotenv -e ../.env -- npx prisma migrate dev",
    "prisma:generate": "dotenv -e ../.env -- npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.8.0"
  }
}
